---
import "../../styles/components/list.styl"

/* TODO: add import wrapper for <List />
 * (if possible)
 */

/* @slot */
type Props = {
    class?: string
}

Astro.props.class = Astro.props.class ? ` ${Astro.props.class}` : ""
---

<ul class=`list-gen list-gen--root${Astro.props.class}`>
    <slot />
</ul>